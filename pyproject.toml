[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "habit-tracker"
version = "0.1.0"
description = "CLI habit tracking backend with SQLite persistence"
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]
